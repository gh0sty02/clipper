ffmpeg-python
yt-dlp>=2025.01.01
opencv-python
numpy
youtube-transcript-api
python-dotenv

# MediaPipe for advanced face detection
mediapipe>=0.10.0

# for using openrouter api
openai

# Optional: Alternative face detection backends (uncomment if needed)
# dlib>=19.24  # For HOG/CNN face detection (requires CMake)
# face-recognition>=1.3.0  # High-level wrapper for dlib (requires dlib)

# Optional: GPU acceleration for MediaPipe (if you have CUDA)
# tensorflow-gpu>=2.10.0  # Only if using GPU inference